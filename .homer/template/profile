if [ ! -z ${homer_debug+x} ]; then echo "Enter ${HOME}/.homer/${HOMER_OS_TYPE}/profile"; fi

. "${HOME}/${HOME}/.homer/${HOMER_OS_TYPE}/bash_profile"

if [ ! -z ${homer_debug+x} ]; then echo "Exit ${HOME}/.homer/${HOMER_OS_TYPE}/profile"; fi
